Clean the cash:

taskkill /f /im python.exe

Reloading:

uvicorn app.main:app --reload


!!!!Підсумок порядку написання

database.py → engine, SessionLocal, get_db

models.py → опис таблиць

schemas.py → Pydantic-схеми для валідації

repository/ → функції для роботи з БД

routers/ → ендпоінти, які викликають репозиторій і отримують db: Session = Depends(get_db)

main.py → підключаємо роутери